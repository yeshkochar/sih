# Database configuration (Defaults to SQLite when blank, Postgres inside docker-compose)
DATABASE_URL=postgresql://fs_user:fs_pass@localhost:5432/freightsense

# Secret key for JWT hashing
SECRET_KEY=sih_super_secure_secret_freight_sense_2026

# Node environment settings
NODE_ENV=development
